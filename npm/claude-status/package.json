{
  "name": "@askviraj/claude-status",
  "version": "0.0.0-managed-by-cargo",
  "description": "A fast powerline status line for Claude Code",
  "keywords": [
    "claude",
    "claude-code",
    "statusline",
    "powerline"
  ],
  "homepage": "https://github.com/virajp/claude-status#readme",
  "bugs": "https://github.com/virajp/claude-status/issues",
  "repository": "github:virajp/claude-status",
  "license": "MIT",
  "author": "Viraj Patel",
  "bin": {
    "claude-status": "./bin/installer.mjs"
  },
  "files": [
    "bin"
  ],
  "os": [
    "darwin"
  ],
  "engines": {
    "node": ">=18"
  },
  "//": "macOS only \u2014 `os` makes npm refuse the install elsewhere rather than failing at run time. optionalDependencies are generated by `mise run build:npm` from the target table in .config/mise/tasks/_scripts/_rust; adding a platform means that table, this `os` field, the PACKAGES map in installer/src/modules/binary.ts, and a build matrix entry in .github/workflows/release.yml."
}
