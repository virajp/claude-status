{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/virajp/claude-status/main/schemas/claude-status.schema.json",
  "title": "claude-status config",
  "description": "Configuration for the claude-status binary. Three layers, deep-merged low to high: the defaults embedded in the binary, ~/.config/claude-status/config.json, and <repo-root>/.config/claude-status.json. Objects merge key by key; arrays and scalars replace wholesale. The repo layer may set projectName and nothing else; any other key in it is ignored, and named by --debug. Nothing in the binary ever writes either file.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "caps": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "context": {
          "type": "integer",
          "maximum": 1000,
          "minimum": 0,
          "description": "Percent of the context window. Default 65.",
          "default": 65
        },
        "fiveHour": {
          "type": "integer",
          "maximum": 1000,
          "minimum": 0,
          "description": "Percent of the 5-hour rate-limit window. Default 90.",
          "default": 90
        },
        "sevenDay": {
          "type": "integer",
          "maximum": 1000,
          "minimum": 0,
          "description": "Percent of the 7-day rate-limit window. Default 80.",
          "default": 80
        },
        "spend": {
          "type": "integer",
          "maximum": 1000,
          "minimum": 0,
          "description": "Percent of the account's monthly budget. Default 90. Only ever breaches on a seat that has a budget block — team and enterprise — and the figure comes from the spend cache the refresh child maintains, never from a fetch on the hook path. Checked before the other three, because a budget does not reset on a timer.",
          "default": 90
        }
      },
      "description": "Thresholds the `--caps-hook` PostToolUse actuator measures usage against, as percentages. When one is exceeded the hook injects a directive telling the agent to hand off and stop, once per escalation. Resolved through all three config layers like any other key, so a repo-level file overrides your user one outright — there is no tighten-only clamp. A key that is absent, negative, non-numeric or above 1000 falls back to its shipped default; `0` is a real cap meaning \"breach on any usage at all\"."
    },
    "projectName": {
      "type": "string",
      "description": "Project display name shown in the `project` segment (with the `symbols.project` glyph before it). **Repo-level only** — it belongs in `<repo-root>/.config/claude-status.json` and is deliberately absent from the shipped defaults, so a repo that has not set one omits the segment rather than inheriting a name from the user layer. When unset, the segment is omitted."
    },
    "palette": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "minItems": 3,
        "maxItems": 3
      },
      "description": "Named colours as 24-bit RGB triples. Referenced by name anywhere a colour is expected."
    },
    "powerline": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cap": {
          "type": "string",
          "description": "Rounded left cap that opens each line."
        },
        "sep": {
          "type": "string",
          "description": "Right-fill triangle between differently-coloured segments."
        },
        "sepThin": {
          "type": "string",
          "description": "Thin divider used when two neighbours share a background."
        },
        "thinFg": {
          "$ref": "#/$defs/color",
          "description": "Foreground colour of the thin divider."
        }
      },
      "description": "Powerline divider glyphs (require a Nerd Font)."
    },
    "defaultFg": {
      "$ref": "#/$defs/color",
      "description": "Foreground colour for segments that don't set their own `fg`."
    },
    "gauge": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "width": {
          "type": "integer",
          "maximum": 1000,
          "minimum": 1,
          "description": "Number of blocks in the gauge."
        },
        "filled": {
          "type": "string",
          "description": "Glyph for filled blocks."
        },
        "empty": {
          "type": "string",
          "description": "Glyph for empty blocks."
        }
      },
      "description": "The fixed-width meter used by the `context` segment."
    },
    "worktreePattern": {
      "type": "string",
      "description": "Case-insensitive regex matched against path components to detect a git worktree checkout; the subpath after the matched component feeds the `worktree` segment."
    },
    "symbols": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Glyph per data type. Keys consumed by the script: model, context, win5h, win7d, reset, session, cost, spend, duration, project, worktree, folder, branch, ahead, dirtyAdd, dirtyDel, dirtyMix, agent, tokens."
    },
    "spend": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "refreshMinutes": {
          "type": "number",
          "minimum": 0,
          "description": "Minimum minutes between fetches of the OAuth usage endpoint (the file-based timer). 0 disables the background refresh entirely; the segment then renders whatever the cache holds. Default 15."
        },
        "show": {
          "type": "string",
          "enum": [
            "auto",
            "always"
          ],
          "description": "`auto` (default) renders only for team/enterprise seats — the plans whose budget is a monthly spend limit. `always` renders whenever budget data exists (e.g. to watch a Pro/Max extra-usage credit cap)."
        }
      },
      "description": "The `spend` segment — the account's monthly budget (claude.ai → Settings → Usage), fetched by a detached background refresh into a machine-global cache (~/.cache/claude-status/spend.json; override with $CLAUDE_STATUS_SPEND_CACHE). A render never fetches."
    },
    "typeSymbols": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Subagent `type` (lower-cased) -> glyph. `_default` is the fallback for unknown types."
    },
    "segments": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/style"
      },
      "description": "Default styling per main-bar segment id (model, context, rl5h, rl7d, session, cost, spend, duration, project, worktree, branch). Overridden inline by an object entry in `lines`."
    },
    "lines": {
      "type": "array",
      "items": {
        "type": "array",
        "items": {
          "$ref": "#/$defs/segmentEntry"
        }
      },
      "description": "Ordered list of rows; each row is an ordered list of segment entries. A row that resolves to no visible segments is dropped."
    },
    "subagent": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "descBudgetFraction": {
          "type": "number",
          "maximum": 1,
          "description": "Fraction of the terminal width allotted to the description before it is truncated.",
          "minimum": 0
        },
        "statuses": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "match": {
                "type": "string",
                "description": "Case-insensitive regex against the lower-cased task status. Empty string = fallback bucket."
              },
              "symbol": {
                "type": "string",
                "description": "Glyph shown for this status."
              },
              "bg": {
                "description": "Head-segment background for this status.",
                "$ref": "#/$defs/color"
              }
            }
          },
          "description": "Status buckets, tried in order. The first whose `match` regex hits the task status wins; an entry with an empty `match` is the fallback for unmatched statuses."
        },
        "segments": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "head": {
              "$ref": "#/$defs/style",
              "description": "Status + type glyph head (background comes from the matched status; bg here is ignored)."
            },
            "name": {
              "$ref": "#/$defs/style",
              "description": "The subagent's name (rendered when the task has one)."
            },
            "model": {
              "$ref": "#/$defs/style"
            },
            "desc": {
              "$ref": "#/$defs/style"
            },
            "tokens": {
              "$ref": "#/$defs/style"
            },
            "duration": {
              "$ref": "#/$defs/style"
            }
          },
          "description": "Styling for each subagent row segment."
        }
      },
      "description": "Configuration for the subagent panel surface."
    }
  },
  "$defs": {
    "color": {
      "description": "A palette name, a hex string (#rgb or #rrggbb), or an RGB triple. `null` clears a colour the defaults set, so the segment falls through to `defaultFg`.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0,
            "maximum": 255
          },
          "minItems": 3,
          "maxItems": 3
        },
        {
          "type": "null"
        }
      ]
    },
    "style": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "bg": {
          "$ref": "#/$defs/color"
        },
        "fg": {
          "$ref": "#/$defs/color"
        },
        "bold": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "segmentEntry": {
      "description": "Either a segment id string, or an object that names a segment and overrides its styling.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "name": {
              "type": "string",
              "description": "Segment id."
            },
            "id": {
              "type": "string",
              "description": "Alias for `name`."
            },
            "bg": {
              "$ref": "#/$defs/color"
            },
            "fg": {
              "$ref": "#/$defs/color"
            },
            "bold": {
              "type": [
                "boolean",
                "null"
              ]
            }
          }
        }
      ]
    }
  }
}
