---
type: vwf-change-plan
title: The project segment names the repository by where it lives
requires: []
backlog: []
---

# Plan — The project segment names the repository by where it lives (2026-09-19)

## Status

**COMPLETE**

COMPLETE 2026-09-19 — U1 fa4401d (+ ac15e99, f3f5fdf, 2de65e6, 6e9037a from the
U5 and R3 loops), U2 3807472, U3 9405fae, U4 735cabe, U6 861b96f, U7 b59e6a7;
folder a1a320b, 3967adf, c498436, a2f5bd1, 5d23138. Folder kept live: gaps G1–G9
open (none blocking; G8 contested at the review cap).

## Consent

| Action                                                   | Granted                                                                                                                                          |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Merge to the integration branch and push on green        | yes                                                                                                                                              |
| After landing: retake `assets/statusline.png` on develop | ask                                                                                                                                              |
| After landing: `mise run code:merge:main`                | ask                                                                                                                                              |
| After landing: `mise run release:tag`                    | ask                                                                                                                                              |
| Release claude-status publicly                           | minor — 1.2.0 → 1.3.0, by hand-editing `Cargo.toml`, refreshing `Cargo.lock` (`cargo update -p claude-status`), and `npm/package.json` alongside |

**The mode recorded here is the consent.** A `run` step runs on a green landing
without a prompt; an `ask` step stops the run once before it, reports what it
would do, and waits. Every after-landing step is `ask`, so the release row is
intent, not authorisation — the executor stops once before each step.

## Goal

After this lands, the `project` segment identifies a repository by **where it
lives**, not by the folder the session happens to be in:

| Situation                              | Glyph key               | Name                                                            |
| -------------------------------------- | ----------------------- | --------------------------------------------------------------- |
| not inside a git repository            | —                       | segment omitted                                                 |
| git, no `origin` remote                | `symbols.projectGit`    | `parent/base` of the identity root, e.g. `virajp/claude-status` |
| `origin` on a host containing `github` | `symbols.projectGithub` | the URL path, e.g. `virajp/claude-plugins`                      |
| `origin` on a host containing `gitlab` | `symbols.projectGitlab` | the URL path, subgroups kept, e.g. `group/sub/repo`             |
| `origin` on any other host             | `symbols.projectRemote` | `parent/base` of the identity root                              |

The **identity root** is the main checkout when the session is in a linked
worktree, the outermost superproject when it is in a submodule, and the checkout
itself otherwise. `projectName` — repo layer, then user layer — still wins, but
it replaces the **name text only**; the glyph stays kind-driven. Branch,
dirty/ahead markers, the `worktree` segment and the repo-config lookup are
unchanged: they still describe the checkout the session is in.

This **amends** three standing decisions in `docs/decisions.md`, both halves
kept and dated:

- `:693` "`project` falls back to the git root's directory name" — the third
  rung is now the identity described above, never a bare directory name.
- `:729-749` "Treat every dynamic value as hostile" — a remote URL read from
  `.git/config` is a **second** clone-controlled string that reaches the bar; it
  passes the same `sanitize`.
- `:1222-1231` "The broken-`.git` asymmetry is deliberate" — the walk is
  untouched; a submodule's *identity* is now resolved separately from its root.

And it **retires** `symbols.project` (the glyph) and the never-read
`symbols.repo`.

The user's words: "Use the current logic for main worktree. Use the folder name
from main worktree when the context switches to another worktree. This doesn't
change the overall logic of first giving priority to statusline config in the
repo." and, on the redesign: "A. Repo is a normal folder (no git) … B. git
folder but without remote … C. git folder with remote (github/gitlab) … D.
currently in a worktree — always use main worktree to detect as B or C."

## Facts the survey established

- **Root resolution is filesystem-only.** `find_root_and_branch()` at
  `src/modules/git.rs:94-108` walks up from `cwd` (`MAX_WALK` = 40, `:21`) via
  `probe()` at `:110-135`: a `.git` *directory* is the root and `HEAD` is read
  there; a `.git` *file* is parsed for `gitdir:` (`:122`), the target is
  normalised lexically against the directory holding `.git` (`:126`,
  `normalise()` `:166-181`), and `HEAD` is read at the target (`:131`). The
  returned root is **the directory holding the `.git` entry** — in a linked
  worktree, the worktree; in a submodule, the submodule. `parse_head()`
  `:147-157`. The module doc (`:1-10`) says root and branch are "never from
  `git`". Subprocesses today: `git rev-list`, `git diff --numstat`,
  `git ls-files` (`:202`, `:214-215`, `:222`), under one 250 ms deadline.
- **Nothing in `src/` reads `.git/config`, `commondir`, or a remote URL.**
  Control: `grep -rni 'commondir\|remote' src/` hit exactly one line,
  `src/modules/config/mod.rs:827` (`remote_agent`, a `typeSymbols` default). No
  git crate: `Cargo.toml` deps are `regex-lite`, `serde`, `serde_json`, `ureq`,
  optional `schemars`; dev-deps `boon`, `tempfile`.
- **`GitFacts`** `git.rs:23-31`: `root`, `branch`, `ahead`, `additions`,
  `deletions`, `worktree_subpath`; derives `Default`. Constructed with
  `..Default::default()` at `src/_runtime/app.rs:290-295` (`build_bar`, then
  `resolve_markers` `:296`) and `:515-520` (`doctor_report_with`, `:521`);
  `git::resolve()` `git.rs:38-46`. Root-only callers: `app.rs:193` (caps hook),
  `:250` (`build_panel`). `find_root_and_branch` call sites: `:193`, `:250`,
  `:272`, `:395`.
- **git.rs tests** `:266-496`: helper `repo(base, head)` `:277-280` writes only
  `<base>/.git/HEAD`. Worktree-shaped tests hand-write the `.git` file:
  `a_worktree_pointer_file_is_followed` `:318` (absolute gitdir, `:326`),
  `a_relative_gitdir_resolves_against_the_directory_holding_dot_git` `:334`
  (`gitdir: ../store/gitdir`, `:342`). Others:
  `a_plain_repo_reports_its_root_and_branch` `:283`,
  `a_broken_head_does_not_stop_the_walk` `:348`,
  `an_unparseable_gitdir_pointer_stops_the_walk_with_a_root_but_no_branch`
  `:378`, `markers_do_not_run_without_a_branch` `:398` (a `GitFacts` literal at
  `:400`), `normalise_resolves_dot_and_dotdot_lexically` `:484`.
- **The segment.** `project()` at `src/modules/render/segments.rs:140-149`,
  dispatched from `text_for` `:117`; doc comment `:127-139`; fallback
  `git.root.file_name()` `:146`; glyph `config.symbol("project")` `:148`. Tests
  `project_reads_the_config_not_the_payload` `:364-369` and
  `project_falls_back_to_the_repo_directory_name` `:372-382` build
  `Config::new(json!({ "symbols": { "project": "P" }, … }))` and a bare
  `PathBuf` root. Other `GitFacts` literals in that test module: `:270`, `:388`,
  `:398-408`, `:425`.
- **Symbols are an open map.** `Config.symbols: BTreeMap<String, String>`
  `src/modules/config/mod.rs:122-125` (`glyph_table` deserializer `:720-725`);
  `Config::symbol` `:189-191` returns `""` for a missing key. Schemars
  description listing the consumed keys at `mod.rs:123`; `projectName`'s at
  `:99` (names `symbols.project` and is already stale on omission). Code
  defaults `default_symbols()` `:795-819` (`("project", "\u{f401}")` `:806`,
  `("repo", "\u{f401}")` `:807`); `Config::default` `:221` (symbols `:239`). The
  pinned twin is `assets/claude-status.defaults.json` `symbols` block `:186-207`
  (`project` `:198`, `repo` `:199`), embedded by `include_str!` at
  `src/modules/config/defaults.rs:20`, marked `-text -diff` in
  `.gitattributes:5` and excluded from dprint (`dprint.json:16`) — **never edit
  it through a formatter**. Agreement is pinned by
  `the_embedded_defaults_deserialize_to_the_default_config` `mod.rs:903-919`.
  Twenty keys today: agent, ahead, branch, context, cost, dirtyAdd, dirtyDel,
  dirtyMix, duration, folder, model, project, repo, reset, session, spend,
  tokens, win5h, win7d, worktree. `symbols.repo` is **read by nothing**
  (`grep symbol("repo")` → nothing).
- **Glyph integrity.** `tests/defaults_integrity.rs` `GLYPHS` table `:33-74`
  (`symbols.project` `:56`, `symbols.repo` `:57`), asserts `GLYPHS.len() == 39`
  (`~:103`), and `the_asset_carries_no_symbol_the_table_does_not_cover`
  (`~:107`) walks every `symbols`/`typeSymbols` key. Nerd Fonts guidance:
  `docs/decisions.md:607-617` "Do not retype the glyphs" — byte-copy codepoints,
  never retype.
- **Schema.** Generated by `schemars` behind `--features schema`
  (`Cargo.toml:92,98`; bin `schema` `:20-22` → `src/bin/schema.rs`, output path
  `:19-21`) into `schemas/claude-status.schema.json` (`projectName` `:47-49`,
  `symbols` `:117-122`). Task `mise run code:schema` (`--check` in the gate).
  Guards in `tests/schema.rs`: `DESCRIPTION_COUNT = 49` (`:46`),
  `DESCRIPTION_DIGEST` (`:67`),
  `the_committed_schema_is_what_the_config_types_generate` `:153`.
  `site/static/config-generator.js` is schema-driven (only a `projectName`
  special-case at `:145`).
- **Config layers.** `layers.rs`: `REPO_LAYER_KEY = "projectName"` `:58`,
  `repo_config_path` `:175-177` (`<root>/.config/claude-status.json`), `load`
  `:187-233` (embedded → user whole → repo narrowed by `narrow` `:268-281`).
  `project_name` deserializer `mod.rs:745-749` (empty → `None`). Untouched by
  this plan.
- **`sanitize`** doc at `src/_shared/text.rs:10-56`; the attacker-nameable list
  `:13-17`; the `projectName` paragraph `:19-29` ("a cloned repository now
  reaches exactly one string on the bar").
- **`--doctor` GIT section** `app.rs:510-524`: `cwd`, `root:` `:512`, `branch:`
  `:513`, `worktree:` `:522`, `ahead:` `:523`, `dirty:` `:524`; values through
  `field()` `:363-365` = `render::sanitize`. Narration `repo root: …` `:275`. No
  test pins the exact line text: `app.rs:952-961` and `tests/e2e.rs:1390` assert
  section names; `e2e.rs:1768` asserts stderr contains `repo root:` under
  `--doctor` (`:1783`).
- **e2e.** `fake_repo(layer)` `tests/e2e.rs:107-115` writes
  `.config/claude-status.json` and a bare `.git/HEAD` — no `git init`, no
  config, no remote. `safe_config()` `:117-119` puts
  `projectName:
  "e2e-fixture"` in the **user** layer, so most bars show
  `e2e-fixture`. The one real-git test:
  `a_dirty_linked_worktree_renders_its_own_dirty_marker` `:1824` (inner `git()`
  helper `:1826-1836`, `git init -q -b main` `:1845`, `worktree add` `:1856`).
  Tests asserting the project text: `:130` (`:137`), `:439` (`:453`), `:909`
  (`:922`), `:1666` (`:1679`), `:2310`, `:2416` (`:2451`, `:2462-2466`),
  `:2583`, `:2669` — every one asserts the **name substring**, none the glyph.
  `:2136` runs with an empty `PATH` and no repo. `tests/golden.rs`: `config()`
  `:25-27` = `layers::load(None, None)`, every `GitFacts` literal leaves
  `root: None` (`:87`, `:101-108`), no golden file contains a project segment.
- **Site glyph font.** `site/static/fonts/claude-status-glyphs.woff2` is a
  `pyftsubset` cut of Hack Nerd Font Mono (Nerd Fonts 3.5.1) down to exactly the
  codepoints the defaults asset uses — "25 glyphs, 3.2KB" —
  `site/static/style.css:84-93` (recipe comment), `unicode-range` list
  `:106-128` (`U+F401` at `:127`); `tests/site.rs:1017` repeats the count in a
  comment and checks the woff2 header's declared length. **No task cuts it.**
  `pyftsubset` is not on PATH; `uvx` 0.12.16 is (mise `uv`);
  `~/Library/Fonts/HackNerdFontMono-Regular.ttf` exists.
- **This repo's own repo layer** `.config/claude-status.json:3` sets
  `projectName: "virajp/claude-status"` — what the GitHub rule now derives.
- **Screenshot.** `assets/statusline.png` (2204×138, retaken at `26efec0`)
  renders the old bare-name segment with U+F401; shown by `readme.md:18-22`,
  `npm/readme.md:20-24`, `site/templates/index.html:41-44`.
- **Gates.** `mise run code:all` = format (dprint), lint
  (`cargo clippy --all-targets -D warnings`, with and without
  `--features schema`), test (`cargo test --features schema`, depends on
  `site:assets`), sec (grype + gitleaks), schema check. Pre-commit and `ci.yml`
  run it on push to `develop`. `mise run site:build` = `zola build` + link
  check. No `harness:` stamp in `.config/vwf.yaml`.
- **Release.** `Cargo.toml:4` `version = "1.2.0"`; `Cargo.lock:100-101` and
  `npm/package.json:3` carry the same string. No bump task: `0fb6ce4`
  hand-edited all three. `mise run release:tag` runs preflight, requires `main`,
  tags `v<Cargo version>`, pushes; `release.yml` publishes. No CHANGELOG;
  `site/` has no release-notes page.
- **Commit convention** (`.config/git-conventional-commits.yaml`): types
  `feat fix perf refactor test docs ops blueprint merge wip`; scopes
  `cli config render subagent spend git usage caps installer plans site spec
  decisions`.
- **Docs the change falsifies** (every hit has an owner below):
  `site/content/segments.md:41` (catalogue row `{sym.project}·my-repo`),
  `:66-78` ("Where `project` comes from", three rungs);
  `site/content/repo-config.md:9-15`, `:26-28` (`<repo-root>` = "the directory
  that contains `.git`"), `:45-56` (`symbols.project`, three rungs);
  `site/content/diagnosing.md:73-77` (GIT block sample), `:136` (already stale
  "No project name" row); `site/content/_index.md:25-26` (feature list omits
  remote parsing — optional); `readme.md:18-22`, `npm/readme.md:20-24`,
  `site/templates/index.html:44` (alt text); `docs/decisions.md:619-637` ("other
  24 codepoints" count), `:693-719`, `:729-749`, `:1205-1215`, `:1222-1231`;
  `schemas/claude-status.schema.json:47-49`, `:122` (generated — fix the source
  strings in `mod.rs`). Not falsified (still true):
  `repo-config.md:17-18,83-95`, `configure.md:74,80`, `generate.md:124-137`,
  `diagnosing.md:116-126`, `tests/fixtures/README.md:19-22`,
  `docs/decisions.md:323-334,484-515`, `docs/usage-mirror-contract.md`.
- **Superseded plan.** `docs/plans/2026-09-19-worktree-project-name/` (index row
  `docs/plans/index.md:24`, APPROVED, never run — its worktree was removed and
  its three commits discarded on 2026-09-19). Its decision 4 said a submodule
  keeps its own name; this plan says the opposite. The folder is deleted by the
  hand-off and its index row removed by `plan-management`.
- **Backlog:** unreadable — no GitHub Project titled `claude-status` exists
  under `virajp`. Nothing recalled; `backlog:` is empty.
- **Glyph codepoints** (from the user, verified 2026-09-19): git U+E702
  (`nf-dev-git`), remote U+F401 (`nf-oct-repo`, already in the subset), GitHub
  U+F09B (`nf-fa-github`), GitLab U+F0BA0 (`nf-md-gitlab`). Net change to the
  subset: +3 codepoints (E702, F09B, F0BA0); F401 stays.

## Assumed decisions — confirm or override at review

| #  | Decision                 | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rejected                                                                                                                           | Unit   |
| -- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1  | Where the identity lives | New `GitFacts.project: Option<Project>` where `pub struct Project { pub kind: ProjectKind, pub name: String, pub root: PathBuf }` and `pub enum ProjectKind { Git, Remote, Github, Gitlab }`, computed by a new `pub fn project(root: &Path) -> Option<Project>` in `git.rs` from the root `find_root_and_branch` returned. `root`, `branch`, the markers, `worktree_subpath` and the repo-config lookup are **unchanged** — they describe the checkout the session is in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Redefine `root` as the main/superproject; resolve branch and markers there too                                                     | U1     |
| 2  | The identity root        | `<root>/.git` is a directory → `root`. It is a file with `gitdir: <p>` (parsed as `probe()` does, normalised against `root`): if `<p>/commondir` reads, the identity root is the **parent** of `normalise(<p>/<commondir contents, trimmed>)`, and a parent whose file name ends in `.git` has that suffix stripped for naming (bare main); else if the normalised `<p>` has a `.git` path component followed by `modules`, the identity root is the directory holding the **topmost** such `.git` component (outermost superproject); else `root`. Lexical only — no subprocess, no symlink resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `git rev-parse --git-common-dir` / `--show-superproject-working-tree`; immediate superproject; treat any `.git` file as a worktree | U1     |
| 3  | Where the remote is read | The git-common config file: `<identity root>/.git/config` for a directory `.git`, or `<p>/<commondir>/config` for a linked worktree, `<topmost .git dir>/config` for a submodule (the superproject's). Parse **`[remote "origin"]`** only; take the first `url =` in that section; ignore every other remote, `pushurl`, and `url.<x>.insteadOf`. No file, no section, no url → kind `Git`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | First remote in file order; the branch's upstream remote; `git remote get-url`                                                     | U1     |
| 4  | URL parsing and kind     | Accept `scp-like` (`[user@]host:path`), `ssh://[user@]host[:port]/path`, `https?://[user@]host[:port]/path`, `git://host/path`, `file://` and bare paths (→ kind `Remote`, no path use). Strip a leading `/`, a trailing `/`, and a trailing `.git`. Host lower-cased: contains `github` → `Github`; contains `gitlab` → `Gitlab`; else `Remote`. A URL that yields no host or an empty path → `Remote`. `Github`/`Gitlab` name = the **full** path (every segment, subgroups kept). `Git`/`Remote` name = `parent/base` of the identity root; a root with no parent component → `base` alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Exact-host match; last two path segments; `Option` on unparseable                                                                  | U1     |
| 5  | Symbol keys              | Flat keys in `symbols`: `projectGit` `\u{e702}`, `projectRemote` `\u{f401}`, `projectGithub` `\u{f09b}`, `projectGitlab` `\u{f0ba0}`. Remove `project` and `repo` from `default_symbols()`, the defaults asset, and the `GLYPHS` table (39 → 41 rows). Update the schemars strings at `mod.rs:99` (drop the `symbols.project` mention and the "omitted when unset" claim) and `:123` (drop `project`, add the four). Regenerate the schema; update `DESCRIPTION_COUNT`/`DESCRIPTION_DIGEST` in `tests/schema.rs` to what the regenerated file yields                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A `projectSymbols` map mirroring `typeSymbols`; keep `symbols.repo`                                                                | U2     |
| 6  | The render               | `project()` returns `None` when `git.project` is `None`; else `format!("{} {name}", config.symbol(key))` with key `projectGit`/`projectRemote`/`projectGithub`/`projectGitlab` by kind and `name = config.project_name.clone().unwrap_or(project.name)`. Same `sanitize` path as today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `projectName` also picking the glyph                                                                                               | U3     |
| 7  | `--doctor`               | GIT section gains one row after `root:`: `project: <kind lower-cased> <name> (<identity root>)`, or `project: <none>` outside git; both values through `field()`. `build_bar` narration gains `project: …` beside `repo root:`. e2e asserts the row is present and names the kind, not the whole line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | No row (the previous plan's ruling 8)                                                                                              | U3     |
| 8  | Tests                    | git.rs: one unit per rule with hand-written layouts under `tempfile` — plain repo (control: kind `Git`, `parent/base`); `.git/config` with an `origin` on `github.com` (scp-like), `gitlab.com` (https, three-segment path), `codeberg.org` (→ `Remote`, `parent/base`); no `origin` but an `upstream` (→ `Git`); linked worktree via `gitdir:` + `commondir` `../..` (→ main's name, and `find_root_and_branch` still returns the worktree); bare main (`repo.git`, suffix stripped); submodule via `gitdir: ../.git/modules/lib` (→ superproject name and superproject's origin); nested submodule (`modules/a/modules/b` → outermost). segments.rs: one render per kind plus `projectName` replacing the name under a `Github` glyph, plus `None` → omit. e2e: real `git init`, `git remote add origin git@github.com:acme/widget.git`, `git worktree add`, and `git -c protocol.file.allow=always submodule add <local path>`, rendering the bar from the main checkout, the worktree and the submodule and asserting glyph + `acme/widget`; the existing `:1824` worktree test is left as-is | Unit tests only                                                                                                                    | U1, U3 |
| 9  | Font subset              | U4 re-cuts `site/static/fonts/claude-status-glyphs.woff2` with `uvx --from fonttools pyftsubset ~/Library/Fonts/HackNerdFontMono-Regular.ttf --unicodes=<comma-joined U+XXXX list of every distinct codepoint under symbols and typeSymbols in the defaults asset> --flavor=woff2 --output-file=…`, updates `unicode-range` in `style.css` to the same list, and rewrites the "25 glyphs, 3.2KB" figures in `style.css:84-93` and `tests/site.rs:1017` to the new count and size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A `site:glyphs` mise task; a manual after-landing step                                                                             | U4     |
| 10 | This repo's repo layer   | Remove `projectName` from `.config/claude-status.json`; if only `$schema` remains, `rm` the file — the GitHub rule derives `virajp/claude-status`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Leave it                                                                                                                           | U2     |
| 11 | Review row               | Present (U5) — the change lands binary source, which executes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | None                                                                                                                               | U5     |
| 12 | Wave 1 concurrency       | U1 (`git.rs`, `text.rs`) and U2 (config, asset, schema, integrity) run together. After wave 1, `config.symbol("project")` returns `""` until U3 lands, so the bar reads `e2e-fixture` with a leading space; every e2e assertion is on the name substring and no golden carries the segment, so the gate stays green                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Serial waves                                                                                                                       | —      |
| 13 | Version bump             | minor, 1.2.0 → 1.3.0, all three files as `0fb6ce4` did                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | patch; none                                                                                                                        | U7     |
| 14 | Decision record          | The three amendments and the retirement go into `docs/decisions.md`, each under the section it amends with today's date and both halves kept — this repo records decisions there, not under `docs/memory/decisions/` (CLAUDE.md, "Where a fact belongs")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A `docs/memory/decisions/` doc                                                                                                     | U6     |
| 15 | After-landing order      | Screenshot first, then merge to `main`, then tag — the retaken PNG must be on `develop` before the merge or it needs a second one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Merge first                                                                                                                        | —      |

## New dependencies

none. U4 runs `fonttools` through `uvx` at run time — nothing is added to any
manifest — and reads `~/Library/Fonts/HackNerdFontMono-Regular.ttf` from this
machine.

## Units

| Id | Wave | Unit file                                    | Kind   | Owns                                                                                                                                                                                                                                                                                                    | Depends on     | Status | Commit  |
| -- | ---- | -------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------ | ------- |
| U1 | 1    | [01-resolution.md](01-resolution.md)         | edit   | `src/modules/git.rs`, `src/_shared/text.rs`                                                                                                                                                                                                                                                             | —              | green  | fa4401d |
| U2 | 1    | [02-symbols.md](02-symbols.md)               | edit   | `src/modules/config/mod.rs`, `assets/claude-status.defaults.json`, `tests/defaults_integrity.rs`, `schemas/claude-status.schema.json`, `tests/schema.rs`, `.config/claude-status.json`                                                                                                                  | —              | green  | 3807472 |
| U3 | 2    | [03-segment.md](03-segment.md)               | edit   | `src/modules/render/segments.rs`, `src/_runtime/app.rs`, `tests/e2e.rs`, `tests/golden.rs`                                                                                                                                                                                                              | U1, U2         | green  | 9405fae |
| U4 | 2    | [04-font.md](04-font.md)                     | edit   | `site/static/fonts/claude-status-glyphs.woff2`, `site/static/style.css`, `tests/site.rs`                                                                                                                                                                                                                | U2             | green  | 735cabe |
| U5 | 3    | [05-review.md](05-review.md)                 | review | —                                                                                                                                                                                                                                                                                                       | U1, U2, U3, U4 | green  | —       |
| U6 | 4    | [06-docs.md](06-docs.md)                     | edit   | `site/content/**`, `site/templates/index.html`, `docs/decisions.md`, `readme.md`, `npm/readme.md`; widened at run time (R1, gap G2): the comment at `.config/mise/tasks/site/assets:24` and the comment at `src/modules/config/write.rs:92` (R4: and the comment at `src/modules/config/schema.rs:215`) | U5             | green  | 861b96f |
| U7 | 5    | [07-gates-and-bump.md](07-gates-and-bump.md) | edit   | `Cargo.toml`, `Cargo.lock`, `npm/package.json`                                                                                                                                                                                                                                                          | U6             | green  | b59e6a7 |

## Shared-file rule

| File                                                                 | Why it collides                       | Owner   |
| -------------------------------------------------------------------- | ------------------------------------- | ------- |
| `Cargo.toml`, `Cargo.lock`, `npm/package.json`                       | version files                         | U7 only |
| `schemas/claude-status.schema.json`                                  | generated; U2 regenerates it once     | U2 only |
| `site/content/**`, `docs/decisions.md`, `readme.md`, `npm/readme.md` | human-facing docs                     | U6 only |
| `src/_runtime/app.rs`                                                | the two `GitFacts` construction sites | U3 only |
| `src/modules/config/mod.rs`                                          | symbol defaults and schemars strings  | U2 only |
| `tests/site.rs`                                                      | the glyph-count comment               | U4 only |

## Waves

- Wave 1: U1 and U2 together — disjoint paths, no shared ruling; decision 12
  says why the gate stays green between them.
- Wave 2: U3 and U4 together — U3 is binary source and tests, U4 is site static
  assets; disjoint.
- Wave 3: U5 alone — reviews the delta since the branch base.
- Wave 4: U6 alone.
- Wave 5: U7 alone.

## Wave gate

```
mise run code:all
mise run site:build
```

plus the wave review, plus every report read for `UNRESOLVED:`. Every line must
be green before wave 1.

## After landing

| Step                                                    | Mode | Notes                                                                                                                                                                                              |
| ------------------------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Retake `assets/statusline.png` and commit it on develop | ask  | Manual: the run reports that the hero screenshot (2204×138, `readme.md`, `npm/readme.md`, `site/templates/index.html`) shows the old segment, and waits for the user to retake, commit and push it |
| `mise run code:merge:main`                              | ask  | lands `develop` on `main` `--no-ff` and pushes; refuses a dirty or unpushed branch                                                                                                                 |
| `mise run release:tag`                                  | ask  | from `main`: runs `release:preflight`, tags `v1.3.0` from `Cargo.toml`, pushes the tag; `release.yml` builds and publishes                                                                         |

## Gates the orchestrator keeps

- **Scratch run**, after wave 2 is green: `cargo build --release`, then under
  `/tmp/cs-scratch/`:
  1. `plain/` — `git init -q -b main`, one commit. Render with `cwd` =
     `/tmp/cs-scratch/plain` → segment reads `<E702> cs-scratch/plain`.
  2. `widget/` — `git init -q -b main`, one commit,
     `git remote add origin git@github.com:acme/widget.git`. Render →
     `<F09B> acme/widget`.
  3. `git -C widget worktree add ../widget-wt -b wt`. Render with `cwd` =
     `/tmp/cs-scratch/widget-wt` → `<F09B> acme/widget`; the `branch` segment
     reads `wt`.
  4. `git -C widget -c protocol.file.allow=always submodule add
     /tmp/cs-scratch/plain lib`.
     Render with `cwd` = `/tmp/cs-scratch/widget/lib` → `<F09B> acme/widget`;
     the `branch` segment reads `lib`'s branch (`main`).
  5. A payload with `cwd` = `/tmp` → no project segment. Feed the binary the
     payload shape `tests/e2e.rs` uses. Pass = all five. Then
     `rm -rf /tmp/cs-scratch`.
- **Site glyphs**, after wave 2: `mise run site:build`, then confirm
  `site/static/fonts/claude-status-glyphs.woff2` declares its own length
  (`tests/site.rs` does this) and that `pyftsubset --unicodes` covered E702,
  F09B and F0BA0 — `uvx --from fonttools ttx -l <woff2>` lists the glyph count;
  it must equal the count U4 wrote into `style.css`.

## Unit contract

Every unit prompt carries, in order: its ruling quoted from this file, its owned
paths plus "touch nothing outside this list", the facts section, the shared-file
rule, and the return block below. A unit never bumps a version, never runs a
generator it is not told to, never edits a doc, never adds a dependency this
file does not list, never commits. A unit deletes with plain `rm`, never
`git rm` — it stages nothing.

A unit returns exactly this block and nothing else — no file contents, no diff:

    CHANGED: <path> — <one line>            (one per file)
    DECIDED: <what> — <why>                 (choices made inside scope, or none)
    DOCS FALSIFIED: <path> — <passage>      (reported, never edited; or none)
    GAP: <what the plan left unspecified and the assumption taken>   (or none)
    UNRESOLVED: <the ruling needed>         (or none)

A `GAP:` is a hole in the plan the unit could proceed past on a stated
assumption; it is recorded and the run continues. An `UNRESOLVED:` is a ruling
the unit could not proceed without; it blocks the unit and its dependents.

## Out of scope

- **Repo-layer config from the main worktree.** `layers.rs:175` reads
  `<root>/.config/claude-status.json` from the checkout the session is in; a
  worktree is a full checkout. Declined by the user 2026-09-19 (twice).
- **`url.<x>.insteadOf` rewrites and non-`origin` remotes.** `origin` only, read
  literally. Declined 2026-09-19.
- **Resolving branch, markers or the `worktree` segment to the main checkout or
  superproject.** Only the project identity resolves there. Declined 2026-09-19.
- **A `site/` release-notes page.** None exists; not asked for.

## Parked

none

## Gaps surfaced during execution

| #  | Source           | Gap                                                                                                                                                                                                                                                                                                                                                                                                                    | Assumption taken                                                                                                                                                                                                                                                                                                                                                                                                                            | Blocking |
| -- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| G1 | U1, R1 round 1   | Ruling 2 says the worktree identity root is "the parent of `normalise(<p>/<commondir>)`" unconditionally, which for a bare main `src/repo.git` names `src`; ruling 2's own "a parent whose file name ends in `.git` has that suffix stripped (bare main)" clause and ruling 8's expected `src/repo` both presume the bare dir itself is the root.                                                                      | The common dir is the identity root unless it is named `.git`, in which case its parent is — the only reading under which both rulings hold. Amend ruling 2's wording at reconciliation.                                                                                                                                                                                                                                                    | no       |
| G2 | R1 round 1       | Two comments no unit owns are falsified by the change: `.config/mise/tasks/site/assets:24` ("the twenty symbols"; the asset now carries 22) and `src/modules/config/write.rs:92` (lists the deleted `.config/claude-status.json` among the `SCHEMA_URL` copies).                                                                                                                                                       | U6's Owns widened to those two passages, authorised by the Goal (the retirement of `symbols.project`/`repo` and ruling 10).                                                                                                                                                                                                                                                                                                                 | no       |
| G3 | U4               | Ruling 9's recipe `uvx --from fonttools pyftsubset … --flavor=woff2` fails with `ImportError: No module named brotli` — woff2 output needs fonttools' `woff` extra.                                                                                                                                                                                                                                                    | Ran `uvx --from 'fonttools[woff]' pyftsubset`/`ttx`; nothing added to any manifest. A future recipe should name `fonttools[woff]`.                                                                                                                                                                                                                                                                                                          | no       |
| G4 | U4               | Ruling 9 reads the codepoint list "under symbols and typeSymbols" (22 glyphs) but expects 25+3=28; the old cut and the recipe comment cover every codepoint in the asset (`powerline.*`, `subagent.statuses.*.symbol` included). The orchestrator gate says `ttx -l` lists the glyph count; it does not.                                                                                                               | Whole-asset scan (28 codepoints); count verified via `ttx -t maxp` (`numGlyphs=29` incl. `.notdef`) and `ttx -t cmap` (28).                                                                                                                                                                                                                                                                                                                 | no       |
| G5 | U3               | Decision 12 claimed every existing e2e assertion on `e2e-fixture` is a name-substring check that survives the change; two (`e2e.rs:137`, `:1679`) rendered the fixture at `/tmp/demo` — no repository — and only passed because the old code drew `projectName` without a root.                                                                                                                                        | Ruling 6 (omit outside git) kept; the two tests render the same payload with `current_dir` swapped for a `fake_repo` path. Confirm at review that `projectName` was never meant to draw outside a repository.                                                                                                                                                                                                                               | no       |
| G6 | U5 round 1       | An `origin` declared through `[include]`/`[includeIf] path = …` in `.git/config` is invisible to the literal `[remote "origin"]` read, so the kind falls to `Git`. Same shape as the declined `url.<x>.insteadOf` (a second file to parse) but not listed under Out of scope.                                                                                                                                          | Not implemented; recorded here for the Out of scope list. U1's loop-back names it in `origin_url`'s doc comment.                                                                                                                                                                                                                                                                                                                            | no       |
| G7 | U5 round 3       | A hidden bare store that no `.git` pointer file names (the dotfiles pattern, `~/.cfg`) holding submodules: `~/.cfg/worktrees/<x>/modules/<sub>` is skipped by the store rule, so the submodule names itself. Probing `HEAD` beside `modules` would catch it but is a new kind of filesystem test the rulings never asked for.                                                                                          | Not fixed; the name drawn is the submodule's own, never a git-internal path. Record beside ruling 2 at reconciliation.                                                                                                                                                                                                                                                                                                                      | no       |
| G8 | U5 round 4 (cap) | **Contested** (cap exit — the plan was not thorough enough): ruling 8's e2e tests run a real `git commit` through `real_git` (`tests/e2e.rs:1899-1908`), which sets `user.name`/`user.email` via `-c` but inherits the developer's global config; with `commit.gpgsign = true` the fixture commits are signed and fail wherever the key is locked or absent. The pre-existing helper at `:1836` has the same exposure. | Not fixed at the cap. Fix is two flags: `-c commit.gpgsign=false -c core.hooksPath=/dev/null` in `real_git` (and, optionally, the `:1836` helper). Decide at the report. Also noted for the site (U6 or a follow-up): an unvouched `.bare` store, a `--separate-git-dir` store's linked worktree and an ssh-alias host (`gh:acme/widget`) draw `projectGit`/`projectRemote` by the rulings — the shapes users will report as "wrong glyph". | no       |
| G9 | U6               | The plan's site examples used a bare `my-repo` (`~/src/my-repo`), which the new rule never draws — the no-origin case is `src/my-repo`. Also noted, pre-existing and untouched: `src/modules/config/write.rs:91-92` still lists `src/_runtime/cli.rs` and `readme.md` among the `SCHEMA_URL` copies, and neither holds the URL.                                                                                        | Site uses `src/my-repo` for the no-origin case and `acme/widget` (the e2e fixture's origin) for GitHub. The stale `write.rs` list is a follow-up outside this plan's Goal.                                                                                                                                                                                                                                                                  | no       |

## Run log

| Wave | Unit              | Model | Round | Outcome     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Commit  |
| ---- | ----------------- | ----- | ----- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 0    | preflight         | —     | 1     | pass        | no `covers:` — format check skipped; no `code` unit — LSP rule and stack conventions skipped; doctor: mise 2026.9.10, graphify CLI and main-checkout `graphify-out/` present, no `projects` block so no stack findings; `mise run code:all` green (432 unit + 15 suites), `mise run site:build` green. Worktree cut from `origin/develop` at `4ea824e` on branch `plan/2026-09-19-project-identity` (EnterWorktree's own branch `worktree-2026-09-19-project-identity` was cut from `main` and is left unused). Sequence: wave 1 U1+U2 (edit) → wave 2 U3+U4 (edit) → wave 3 U5 (review, covers U1–U4) → wave 4 U6 (edit) → wave 5 U7 (edit); each wave closes with R<n> and the gate lines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —       |
| 1    | U1 resolution     | opus  | 1     | pass        | node edit. `git.rs` gains `ProjectKind`, `Project`, `GitFacts.project`, `pub fn project()` (`identity_root`/`origin_url`/`classify`/`parent_base`), `gitdir_pointer` shared with `probe()`, 13 tests; `text.rs` sanitize doc names the remote URL. Suite 445 unit + every suite green, clippy clean both ways. DECIDED: `gitdir_pointer(dir, text)` takes the pointer text so `probe()` keeps Continue vs RootOnly; worktree identity root = parent of the common dir when named `.git`, else the common dir itself; a pointer with neither `commondir` nor `.git/modules` reads `config` at the gitdir it points to; `parent_base` strips a trailing `.git` from any root's base; `classify` returns the stripped path for `Remote` hosts but `project()` uses `parent/base`. GAP: ruling 2's literal "parent of normalise(<p>/<commondir>)" contradicts ruling 8's expected `src/repo` for a bare `src/repo.git` main — the test's expectation was taken as the contract. DOCS FALSIFIED: none beyond U6's list.                                                                                                                                                                                                                                                                                                                                                                                                                                             | fa4401d |
| 1    | U2 symbols        | opus  | 1     | pass        | node edit. `default_symbols()` drops `project`/`repo`, adds `projectGit` E702, `projectGithub` F09B, `projectGitlab` F0BA0, `projectRemote` F401; schemars strings at `projectName`/`symbols` rewritten; asset edited by hand (glyphs emitted from hex, alphabetical position), `GLYPHS` 39 → 41, schema regenerated (two description strings only), `DESCRIPTION_DIGEST` → `0x4bbc_8140_5600_681d` (`COUNT` stays 49), `.config/claude-status.json` removed (only `$schema` remained). DECIDED: `default_symbols()` entries kept where `project`/`repo` sat (list is not alphabetical; `BTreeMap` orders it); `DESCRIPTION_DIGEST` doc comment not extended. GAP: none.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 3807472 |
| 1    | R1                | opus  | 1     | findings(7) | CONTRACT clean. RULINGS: U1 departed from #2 (identity root = common dir unless named `.git`; literal ruling says parent unconditionally) — ruled by the orchestrator as the plan contradicting itself: ruling 2's own "strip `.git` for a bare main" clause presumes the bare dir is the root, and ruling 8 expects `src/repo`; U1's reading kept, recorded as gap G1, not looped. Looped: `git.rs:204-205` fold width → U1; `tests/schema.rs:66` digest "moved from" paragraph and `:73` "twenty symbols" → U2. Accepted as DECIDED: `gitdir_pointer(dir, text)` signature (behaviour identical); `git.rs:45` unbreakable test name at 85 cols. Rule-5 in unowned files → `DOCS FALSIFIED` handed to U6, Owns widened (gap G2): `.config/mise/tasks/site/assets:24` "the twenty symbols"; `src/modules/config/write.rs:92` lists the deleted `.config/claude-status.json` among `SCHEMA_URL` copies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —       |
| 1    | U1 resolution     | opus  | 2     | pass        | node edit (R1 loop-back). `identity_root` doc paragraph rewrapped to ≤80 cols; `git::` tests 33 pass, clippy clean both ways.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | fa4401d |
| 1    | U2 symbols        | opus  | 2     | pass        | node edit (R1 loop-back). `tests/schema.rs`: "Moved again from `0xd2cc_8890_f890_7b7f` by `project-identity` (2026-09-19)" paragraph naming the two rewritten strings; prior paragraph's "only additions" scoped "— for that move"; "twenty" → "twenty-two". Schema suite 12 pass. DECIDED: `cargo fmt --check` pre-existing diffs in `src/_runtime/*`, `src/bin/schema.rs` are not this unit's; the gate is dprint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 3807472 |
| 1    | R1                | opus  | 2     | pass        | FINDINGS 0, CONTRACT clean, RULINGS clean (ruling-2 reading stands as gap G1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —       |
| 2    | U4 font           | opus  | 1     | pass        | node edit. woff2 re-cut from HackNerdFontMono-Regular.ttf over the 28 PUA codepoints the asset uses (+E702, F09B, F0BA0): 29 glyphs incl. `.notdef`, 4264 bytes; `style.css` `unicode-range` = the 28-codepoint list, recipe comment "28 glyphs, 4.2KB"; `tests/site.rs:1017` comment updated, no assertion changed. `--test site` 28 pass, `site:build` green. DECIDED: scanned the whole asset, not only `symbols`/`typeSymbols` — the literal ruling-9 filter yields 22 and drops six the old cut carried (`powerline.*`, `subagent.statuses.*.symbol`); whole-asset gives the plan's own 25+3=28. Excluded U+FE0F and U+1F332 (no outline in Hack). e2e failures seen mid-run are U3's in-flight files. GAP: `pyftsubset --flavor=woff2` needs `fonttools[woff]` (brotli) — used that, nothing added to a manifest (G3); `ttx -l` prints no glyph count — verified via `ttx -t maxp`/`-t cmap` (G4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 735cabe |
| 2    | U3 segment        | opus  | 1     | pass        | node edit. `segments.rs` `project()` draws `symbol(glyph_key(kind)) + name` from `git.project`, `projectName` replaces the name only, `None` omits; three new render tests replace the two old ones. `app.rs`: both `GitFacts` sites set `project: root.as_deref().and_then(git::project)`, narration gains `project:`, `--doctor` GIT gains `project: <kind> <name> (<root>)`/`<none>` after `root:`. `e2e.rs`: helpers `real_git`/`git_on_path`/`unnamed_config`/`payload_in`/`github_fixture` and three tests (origin from checkout+worktree+submodule; no-origin `parent/base`; doctor row names the kind); `:1824` untouched. `golden.rs` unchanged (all literals `..Default::default()`). Suite 683 tests / 15 suites green, clippy clean both ways, `symbol("project")` gone. DECIDED: `glyph_key` private (doctor prints the kind name); new e2e tests skip with `eprintln!` when `git --version` fails. GAP (G5): two existing e2e tests rendered `FIXTURE` at `/tmp/demo` and asserted `e2e-fixture` drawn from `projectName` alone; ruling 6 kept, tests re-pointed at a `fake_repo`.                                                                                                                                                                                                                                                                                                                                                               | 9405fae |
| 2    | R2                | opus  | 1     | pass        | FINDINGS 0, CONTRACT clean, RULINGS clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —       |
| 2    | gate              | —     | 1     | pass        | Wave gate: `code:all` green (446 unit + 15 suites, e2e 91), `site:build` green. Orchestrator gates: scratch run of the release binary — plain → E702 `cs-scratch/plain`; widget → F09B `acme/widget`; worktree → F09B `acme/widget`, branch `wt`; submodule → F09B `acme/widget`, branch `main`; `/tmp` → no segment: 5/5 pass, scratch removed. Site glyphs: cmap of the woff2 lists 28 codepoints incl. E702/F09B/F0BA0, equal to `style.css` "28 glyphs"; 4264 bytes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —       |
| 3    | U5                | opus  | 1     | pass        | node security. Range `4ea824e..3967adf`; file list = 14 paths, each mapped to the one unit whose commit touched it (U1 fa4401d: `git.rs`, `text.rs`; U2 3807472: `config/mod.rs`, asset, `defaults_integrity.rs`, schema, `schema.rs`, `.config/claude-status.json`; U3 9405fae: `segments.rs`, `app.rs`, `e2e.rs`; U4 735cabe: woff2, `style.css`, `site.rs`); `a1a320b`/`3967adf` excluded as folder commits. Engine `/security-review`: NO FINDINGS (`engine/U5-1.security.log`). Reviewer: NO FINDINGS above Info; verified sanitize chokepoints (`segments.rs:55`, `field()` `app.rs:365`, `sanitize_report` `:554`, `diag` `_shared/mod.rs:48`), origin authority dropped in `classify`, four file reads, no `Command`/`canonicalize`, no panic on hostile input. Info residuals accepted by ruling: substring host match spoofs the glyph; `parent/base` draws the parent dir; lexical pointer follow unconfined (bounded disclosure). Tag `U5/security/1`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       |
| 3    | U5                | opus  | 1     | findings(4) | node review. Same range/file list/map as the security node. Engine `/code-review` high: 6 findings, all `src/modules/git.rs` (`engine/U5-1.review.log`). Reviewer kept 3 and folded 2, dropped 1 — all labelled (U1), covered: (1) Medium `identity_root:228` `.bare`-pointer worktree layout draws `widget/.bare` — open under G1, which weighed `repo.git` only; (2) Medium `identity_root:236` a submodule inside a linked worktree (`.git/worktrees/<wt>/modules/<sub>`, no `commondir`) names itself, contradicting the Goal's "outermost superproject"; (3) Low `identity_root:228` a linked worktree of a submodule names `modules/<sub>` from the git-internal common dir; (4) Low `origin_url:254-263` header/key case-sensitive, quoted values and trailing `#`/`;` comments unhandled — not fixed by ruling 3. Dropped: `[include]`-declared origin — same shape as `insteadOf`; recorded as gap G6 for Out of scope and `origin_url`'s doc comment. CONTRACT clean (05-review.md points all verified), RULINGS clean. Loop-back → U1. Tag `U5/review/1`.                                                                                                                                                                                                                                                                                                                                                                                           | —       |
| 3    | U1 resolution     | opus  | 3     | pass        | node edit (U5 round-1 loop-back). `identity_root` restructured: commondir resolved first, then a shared `superproject()` scan (`.git/modules` or `.git/worktrees/<x>/modules`, topmost wins) applied to the common dir, then hidden common dirs (`.git`, `.bare`, any dot-name) take their parent; `origin_url` matches `remote`/`url` case-insensitively, strips a quoted pair, cuts `#`/`;` comments via `config_value()`, doc notes `[include]`/`[includeIf]` are not followed; four new tests. Suite 450 unit + every suite green, clippy clean both ways. DECIDED: subsection `"origin"` stays case-sensitive as in git; a quoted value is taken verbatim to its closing quote; `#`/`;` not preceded by whitespace is part of the value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ac15e99 |
| 3    | U5                | opus  | 2     | pass        | node security. Range `4ea824e..ac15e99`; `git.rs` → U1 via `ac15e99`. Engine `/security-review`: NO FINDINGS (`engine/U5-2.security.log`). Reviewer: NO FINDINGS above Info; `superproject()` is a bounded prefix slice with no read; dot-name rule is display-only; reads unchanged (pointer, `HEAD`, `commondir`, `<common>/config`); quotes stripped before `classify` so authority is still discarded; no panic on `""`/`"`/`#`; no `Command`/`canonicalize`; `modules::git::` 37 pass. Info residual accepted: a credentialed URL with an unencoded `/` in the password mis-parses in git too. Tag `U5/security/2`, room `problems`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —       |
| 3    | U5                | opus  | 2     | findings(3) | node review. Range `4ea824e..ac15e99`. Round-1 findings 1–4 confirmed resolved by `ac15e99` (re-probed with real-git layouts). Engine `/code-review` high: 4 new findings (`engine/U5-2.review.log`, none seen in round 1). Reviewer kept all, folded to 3, all (U1): (1) Medium `identity_root:242-256` + `superproject:258-268` — the two store rules disagree: `superproject()` keys on a literal `.git`, so a submodule under a `.bare` or `repo.git` store names itself, and the dot-name rule sends a hidden store outside any checkout (`~/.cfg`) to `$HOME`; one ruling asked for: store component S ∈ {`.git`, dot-name, `*.git`}, `store_identity(S)` = parent when S is `.git` or a dot-name whose `<parent>/.git` pointer names S, else S; `superproject()` matches topmost S before `modules`/`worktrees/<x>/modules`; (2) Low `origin_url:286-293` a header with a trailing comment closes the section — cut at `]`; (3) Low `config_value:314-330` cut `#`/`;` at any unquoted position as git does (less code), or rename the test. Convergence guard: 4 → 3, none resurfaced — converging, round 3 allowed (cap 4). Loop-back → U1. Tag `U5/review/2`, room `problems`.                                                                                                                                                                                                                                                                       | —       |
| 3    | U1 resolution     | opus  | 4     | pass        | node edit (U5 round-2 loop-back). `is_store()` (`.git`, dot-name, `*.git`) and `store_identity()` (parent for `.git`, or for a dot-name whose `<parent>/.git` pointer normalises to it; else the store itself) replace the dot-name rule; `superproject()` matches the topmost store before `modules`/`worktrees/<x>/modules`; commondir branch applies `store_identity(common)`; `origin_url` cuts the header at the first `]`; `config_value` cuts an unquoted value at the first `#`/`;`; two tests added, the config test extended. Suite 452 unit + every suite green, clippy clean both ways. DECIDED: the one added read is `<parent>/.git`, lexical; a `[` line with no `]` still closes the section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | f3f5fdf |
| 3    | U5                | opus  | 3     | pass        | node security. Range `4ea824e..f3f5fdf`; `git.rs` → U1 via `f3f5fdf`. Engine `/security-review`: NO FINDINGS (`engine/U5-3.security.log`). Reviewer: NO FINDINGS above Info — the one new read (`<parent>/.git`) is compare-only and within the pointer chain's existing reach; `is_store` on any dot-name yields only prefixes of a path `commondir` already named; `config_value` cuts only shorten and quotes are stripped before `classify`; `..` store → no segment, no panic; six `read_to_string` sites total, no `Command`/`canonicalize`; `modules::git::` 39 pass. Tag `U5/security/3`, room `problems`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       |
| 3    | U5                | opus  | 3     | findings(1) | node review. Range `4ea824e..f3f5fdf`. Round-2 findings 1–3 confirmed resolved by `f3f5fdf` (re-probed). Engine `/code-review` high: 1 new Low (`engine/U5-3.review.log`). Reviewer kept it, widened, (U1): `is_store:252-255`/`superproject:283-291` accept a hidden ancestor before a literal `modules` as a store unverified — a submodule under `~/.emacs.d/modules/foo` draws `home/.emacs.d`, and so does a plain linked worktree whose main checkout lives there. Ruling: a candidate store S before `modules`/`worktrees/<x>/modules` is accepted only when S is `.git`, `*.git`, or a dot-name whose `<parent>/.git` pointer names S — lift `store_identity`'s test into `checkout_of(store)`, skip unverified candidates and fall through to later ones; no `HEAD` probe. Residual recorded as G7 (`~/.cfg` + submodules names the submodule). Convergence guard: 3 → 1, none resurfaced — round 4 allowed (the cap). Loop-back → U1. Tag `U5/review/3`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       |
| 3    | U1 resolution     | opus  | 5     | pass        | node edit (U5 round-3 loop-back). `is_store` removed; `checkout_of(store)` holds the one rule (`Some(parent)` for `.git` or a pointer-verified dot-name); `store_identity` = `checkout_of(..).unwrap_or(store)`; `superproject` iterates candidates topmost-first and accepts the first `checkout_of` vouches for or that ends in `.git`; test `a_hidden_ancestor_named_before_modules_is_not_a_store` (submodule and linked worktree of `<tmp>/.emacs.d/modules/foo` → `Github` `me/foo`). Suite 453 unit + every suite green, clippy clean both ways. GAP: G7 restated (unverified `~/.cfg` store holding submodules → the submodule names itself).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2de65e6 |
| 3    | U5                | opus  | 4     | pass        | node security. Range `4ea824e..2de65e6`; `git.rs` → U1 via `2de65e6`. Engine `/security-review`: NO FINDINGS (`engine/U5-4.security.log`). Reviewer: NO FINDINGS above Info — the per-candidate `<parent>/.git` reads land at strict prefixes of the attacker-named `common`, each consumed as one bool; bar/`--doctor` surface unchanged; no panic paths; six `read_to_string` sites total, no `Command`/`canonicalize`; `modules::git::` 40 pass. Env note: a `1.98.0` rustup toolchain on this machine is missing its manifest; the reviewer ran on `1.98.1`. Tag `U5/security/4`, room `problems`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —       |
| 3    | U5                | opus  | 4     | findings(1) | node review (the cap). Range `4ea824e..2de65e6`. Round-3 finding resolved by `2de65e6` (re-probed: `~/.emacs.d/modules/foo` submodule and worktree → `me/foo`; every earlier layout unchanged). Engine `/code-review` high: 4 Lows (`engine/U5-4.review.log`). Reviewer kept 1, dropped 3: kept (U3) Low `tests/e2e.rs:1899-1908` `real_git` inherits the global git config — this machine has `commit.gpgsign = true`, so the fixture commits sign; fails where the key is locked/absent; fix `-c commit.gpgsign=false -c core.hooksPath=/dev/null` (the pre-existing helper at `:1836` shares the exposure) → **contested** at the cap, gap G8. Dropped: `foo.git`-named non-bare checkout hosting a nested repo (residual, contrived); a diagnostic for retired `symbols.project`/`repo` (premise false — `validate.rs:263` already reports them `Unread`; migration is out of scope); `write.rs:92` (G2/U6). Residuals the rulings accept, noted for the site: an unvouched `.bare` store, a `--separate-git-dir` store's worktree, ssh-alias hosts (`gh:acme/widget`) draw `projectGit`/`projectRemote`. Guard: 1 → 1 on a different unit; the cap ends the loop. Row **green**. Tag `U5/review/4`.                                                                                                                                                                                                                                                       | —       |
| 3    | R3                | opus  | 1     | findings(3) | Wave diff `3967adf..2de65e6` = U1's three fix commits, `git.rs` only. CONTRACT clean, RULINGS clean; pre-existing tests byte-unedited, seven tests added; `find_root_and_branch`/`probe`/`resolve_markers`/`Project` API untouched; the only `HEAD` read is `probe()`'s. Looped to U1 (docs): `identity_root` bullet `:207-209` still says "topmost such `.git`"; `gitdir_pointer` doc `:179-180` omits `checkout_of` as a caller. Noted only: `config_value` treats only a leading `"` as quoting (`x"#"y` cut at `#`), already recorded in its doc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —       |
| 3    | U1 resolution     | opus  | 6     | pass        | node edit (R3 loop-back, docs only). `identity_root` bullet states the store rule; `gitdir_pointer` doc names `checkout_of`; ≤80 cols; no code change; `git::` 40 pass, clippy clean. Lands after U5's last round → U5 late re-run 1 pending over this commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 6e9037a |
| 3    | R3                | opus  | 2     | pass        | FINDINGS 1 noted, no re-dispatch: `config_value` recognises only a leading `"` as quoting (narrower than "first unquoted `#`/`;`"), recorded in its doc; no URL shape reaches it — carried as a noted residual, not looped. CONTRACT clean, RULINGS clean; `6e9037a` doc-only, both round-1 findings closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —       |
| 3    | U5                | opus  | 1     | pass        | node review, **re-run 1** (`U5-late1`, trigger: R3's doc loop-back). Range `2de65e6..6e9037a`; file list `src/modules/git.rs` → U1 (`6e9037a`, doc-only +8/-6). Engine `/code-review` high: 3 lows, all dropped at filter — 2 engine findings already reported by U5 (round-4 residuals: `--separate-git-dir` store, unvouched `*.git`), 1 outside the range (U6's `repo-config.md:46`) (`engine/U5-late1-1.review.log`). Reviewer: FINDINGS 0; both comments now say what the code does. Tag `U5-late1/review/1`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       |
| 3    | U5                | opus  | 1     | pass        | node security, **re-run 1** (`U5-late1`). Range `2de65e6..6e9037a`. Engine `/security-review`: NO FINDINGS, doc-only (`engine/U5-late1-1.security.log`). Reviewer: NO FINDINGS; 0 non-comment lines changed, verdicts from `U5/security/1–4` stand. Tag `U5-late1/security/1`. Re-run 1: 1 round, clean; produced no fix commit, so `R3-late` has no delta — R3 round 2 already reviewed `6e9037a` for contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —       |
| —    | acceptance        | —     | 1     | skipped     | why: plan has no `covers:` — a change plan carries no blueprint acceptance criteria.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —       |
| —    | ux                | —     | 1     | skipped     | why: plan has no `covers:` — no Screens contract to verify; the site's font/CSS change was gated by the woff2 cmap check and `site:build`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —       |
| —    | reconcile         | —     | 1     | skipped     | why: plan has no `covers:` — no registry, environment, harness or `implementation:` stamp to write; no `code` unit — nothing to persist beyond the `DECIDED:` lines already in this log and the gaps filed to room `gaps`. No Reconcile commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —       |
| 4    | U6 docs           | opus  | 1     | pass        | node edit. `segments.md` catalogue row `{sym.projectGithub}·acme/widget`, "Where `project` comes from" = five-row table + identity-root paragraph + `origin`/substring note with the three residual shapes + `projectName` name-only + three e2e test names; `repo-config.md` intro, `<repo-root>` sentence, four keys, third rung; `diagnosing.md` GIT sample `project:` row, symptom row → "Wrong project name or glyph"; `_index.md` feature bullet; alt text in `readme.md`, `npm/readme.md`, `index.html`; `docs/decisions.md` dated amendments under the three sections + "Filesystem first" + the codepoint count, new "`symbols.project` and `symbols.repo` retired" section; G2: `site/assets` comment → twenty-two, `write.rs` comment drops `.config/claude-status.json`. `site:build`, `--test site` 28, `code:lint/test(690)/sec/schema` green individually. DECIDED: docs-sync surveyor ran but returned no report to the unit (its report reached the orchestrator instead — every finding was already on U6's list), so the survey was redone by hand; `configure.md:74` generic, left; symptom row kept short for dprint. DOCS FALSIFIED (pre-existing, not this run's — noted, not fixed): `write.rs:91-92` still lists `cli.rs` and `readme.md` as `SCHEMA_URL` copies; neither holds the URL. GAP (G9): plan examples said `my-repo`; the rule draws `src/my-repo`, so the site uses `src/my-repo` (no origin) and `acme/widget` (GitHub). | 861b96f |
| 4    | R4                | opus  | 1     | findings(4) | CONTRACT clean (comment-only edits in the two G2 files; `decisions.md` deletes no earlier text; edits 1–8 landed; cited e2e tests exist), RULINGS clean. Looped to U6: `segments.md:89` and `decisions.md:736-737` claim two of the three residual shapes draw the wrong glyph — false, they draw `projectGithub` from the store's `config`; the residual is the **name** without an origin, only the ssh alias is a wrong-glyph case (the orchestrator's briefing carried the reviewer's wording; verified against `--doctor`); `decisions.md:790` inventory omits the `checkout_of` `<parent>/.git` probes; `decisions.md:630` "per-host" mislabels U+E702. Rule 5 in an unowned file → U6 Owns widened again under G2: `src/modules/config/schema.rs:215` "all twenty symbols".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       |
| 4    | U6 docs           | opus  | 2     | pass        | node edit (R4 loop-back). `segments.md` residual passage: right glyph from the store's `origin`, residual is the `parent/base` name without an origin; only the ssh alias is a wrong-glyph case; `decisions.md` `:736-737` same, `:790` inventory gains the per-candidate `<parent>/.git` probe, `:630` "three new project glyphs"; `schema.rs:215` "twenty-two" (G2). dprint clean, `site:build` green, `--test site` 28, clippy clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 861b96f |
| 4    | R4                | opus  | 2     | pass        | FINDINGS 0, CONTRACT clean, RULINGS clean; round-1 findings closed, `decisions.md` deletes no earlier text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —       |
| 5    | U7 gates-and-bump | opus  | 1     | pass        | node edit. `Cargo.toml:4` → 1.3.0; `Cargo.lock` `claude-status` block via `cargo update -p claude-status --offline`, one line; `npm/package.json:3` → 1.3.0. `code:all` green, `site:build` green; `release:preflight` not run (needs `main`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | b59e6a7 |
| 5    | R5                | opus  | 1     | pass        | FINDINGS 0, CONTRACT clean, RULINGS clean; three files, one line each; `Cargo.lock` moved only the `claude-status` block; schema unchanged; taplo/dprint clean; no `1.2.0` in the docs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —       |
| —    | reconcile         | —     | 2     | pass        | Final gate over the finished tree at `b59e6a7`: `mise run code:all` green (453 unit + 15 suites; 690 tests in all), `mise run site:build` green. Gates the orchestrator keeps, re-run on the 1.3.0 release build: scratch run 5/5 (plain → E702 `cs-scratch/plain`; widget/worktree/submodule → F09B `acme/widget` with branches `main`/`wt`/`main`; `/tmp` → no segment), scratch removed; woff2 cmap 28 codepoints = `style.css` "28 glyphs". Model downgrades: none — every node ran on opus. Knobs: `review_round_cap` default 4 (no `pipeline` block in `.config/vwf.yaml`), reached on U5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —       |

## Launch

This folder is already committed and pushed on the branch it was planned on, so
the fresh session's worktree — cut from the integration branch — can see it.

Run in a fresh session:

/vwf:execute docs/plans/2026-09-19-project-identity

or let the queue pick it, by priority:

/vwf:execute next
