#!/usr/bin/env bash
#MISE description="Clean the build artifacts"

set -e
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/_helpers"

print_header "Cleaning build artifacts"
[ -d target ] && rm -rf target

exit 0
